services:
  travel-rs:
    # Version tag pinned by the release.yml workflow
    image: ghcr.io/princeofborgo/travel-rs:v0.3.1
    container_name: travel-rs
    volumes:
      - ./config:/app/config
      - ./locales:/app/locales
      - ./logs:/app/logs
    restart: always